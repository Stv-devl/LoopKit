# DESIGN.md

Pointer only. The design system lives in `docs/design-system.md`, written by
`/loop:design-system`. Impeccable reads this file; do not run
`/impeccable document` over it and do not copy the system here.
